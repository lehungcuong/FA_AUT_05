using OpenQA.Selenium;
using System;
using System.Collections.Generic;
using System.Threading;
using TestPOM.Pages;
using TestPOM.Test;
using TestPOM2;
using Xunit;

namespace TestPOM.Test
{
    
    public class ClassData : Basetest
    {

      /*  private readonly Tourspage tourspage;
        private readonly Homepage homepage;

        public ClassData()
        {
            homepage = new Homepage(browserFactory.Driver);
            tourspage = new Tourspage(browserFactory.Driver);
        }

       

        public IEnumerator<object[]> GetEnumerator(string Firstname, string Lastname, string Email, string Phone, string Address,
          string FirstName1, string ClassName1, string FirstName2, string ClassName2)
        {
            yield return new object[] { new object[]
            { 
                "Quang", "Tran", "quangtran@gmail.com", "123456789", "83/2C", "Quang", "Tran", "Quang", "Tran"}
            };
           
        }
        public void BookToursInCLassData(string Firstname, string Lastname, string Email, string Phone, string Address,
         string FirstName1, string ClassName1, string FirstName2, string ClassName2)
        {
            homepage.bnttours();
            Thread.Sleep(2000);
            //input infomation of user 
            tourspage.Bookingtoursbyindata(Firstname, Lastname, Email, Phone, Address, FirstName1, ClassName1, FirstName2, ClassName2);
        }

        */
    }


}
